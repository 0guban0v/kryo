[project]
name = "kryo-mlx"
version = "0.1.0"
description = "Local host-side model runtime for Kryo."
requires-python = ">=3.11"
dependencies = [
  "vllm-mlx",
  "anthropic",
  "requests"
]

[tool.uv]
package = false
